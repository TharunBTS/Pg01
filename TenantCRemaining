@PostMapping("/{tenantId}/occupy/{pgId}")
public Tenant occupyPg(@PathVariable Long tenantId,
                       @PathVariable Long pgId) {
    return tenantService.assignPgToTenant(tenantId, pgId);
}
